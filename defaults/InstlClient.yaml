--- !define

SYNC_TRAGET_OS_URL: $(SYNC_BASE_URL)/$(TARGET_OS)
BOOKKEEPING_DIR_URL: $(SYNC_BASE_URL)/instl
INFO_MAP_FILE_URL: $(SYNC_BASE_URL)/$(REPO_REV)/instl/info_map.txt

PARALLEL_SYNC: 16
CURL_CONFIG_FILE_NAME: curl-config.txt
CURL_CONNECT_TIMEOUT: 12
CURL_MAX_TIME: 360
CURL_RETRIES: 12

LOCAL_BOOKKEEPING_PATH: $(LOCAL_SYNC_DIR)/bookkeeping
HAVE_INFO_MAP_PATH: $(LOCAL_BOOKKEEPING_PATH)/have_info_map.txt
NEW_HAVE_INFO_MAP_PATH: $(LOCAL_BOOKKEEPING_PATH)/new_have_info_map.txt
REQUIRED_INFO_MAP_PATH: $(LOCAL_BOOKKEEPING_PATH)/required_info_map.txt
TO_SYNC_INFO_MAP_PATH: $(LOCAL_BOOKKEEPING_PATH)/to_sync_info_map.txt
REPO_REV_LOCAL_BOOKKEEPING_PATH: $(LOCAL_BOOKKEEPING_PATH)/$(REPO_REV)
LOCAL_COPY_OF_REMOTE_INFO_MAP_PATH: $(REPO_REV_LOCAL_BOOKKEEPING_PATH)/remote_info_map.txt
INSTL_HISTORY_PATH: $(LOCAL_BOOKKEEPING_PATH)/instl_history.txt
INSTL_HISTORY_TEMP_PATH: $(LOCAL_BOOKKEEPING_PATH)/instl_history.temp.txt
XCOPY_EXCLUDE_FILE_NAME: xcopy-exclude.txt
XCOPY_EXCLUDE_FILE_PATH: $(LOCAL_SYNC_DIR)/$(XCOPY_EXCLUDE_FILE_NAME)

COPY_IGNORE_PATTERNS:
    - ".svn"
    - "*.symlink"
    - "*.wtar"
    - "*.wtar.??"
    - "*.done"